#!/usr/bin/env Rscript
png(file="/mnt/rwizflowy-bucket/prod/betterment/plot1.png")
plot(1:sample(1:100,1))
dev.off()

png(file="/mnt/rwizflowy-bucket/prod/betterment/plot2.png")
plot(1:sample(1:100,1))
dev.off()